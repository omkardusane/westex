
describe('AccountService Integration Tests', () => {
    // let accountSvc: AccountService;

    beforeAll(async () => {
        // Set up DB connection & Accounts service
        // accountService = new AccountService();
    });

    afterAll(async () => {
        // Clean up DB
        // await Database.disconnect();
    });

    it('should create and retrieve an account from the database', async () => {

    });

    it('should not allow creation of an account for existing user', async () => {

    });

    it('should confirm if the user is valid or not', async () => {

    });
});
